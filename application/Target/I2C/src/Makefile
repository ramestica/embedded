EXECUTABLES =
LIBRARIES   = TargetI2C
INCLUDES    = TargetI2C.h

TargetI2C_SOURCES = I2C_$(PROCESSOR).cpp
TargetI2C_LIBS    =

include $(EMBEDDED_ROOT)/include/Makefile.mk

all: do_all
	@echo " . . . 'all' done" 

install: do_install
	@echo " . . . 'install' done" 

upload: do_upload
	@echo " . . . 'upload' done" 

clean: do_clean
	@echo " . . . 'clean' done" 

#___oOo___
