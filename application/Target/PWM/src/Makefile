EXECUTABLES =
LIBRARIES	= TargetPWM
INCLUDES_TARGET = PWM.h

#DEBUG=on

TargetPWM_SOURCES = PWM.cpp
_LIBS    =

include $(EMBEDDED_ROOT)/include/Makefile.mk

all: do_all
	@echo " . . . 'all' done" 

install: do_install
	@echo " . . . 'install' done" 

upload: do_upload
	@echo " . . . 'upload' done" 

clean: do_clean
	@echo " . . . 'clean' done" 

#___oOo___
