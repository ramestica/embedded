EXECUTABLES_L = sandbox cmsis
EXECUTABLES_L = sandbox
LIBRARIES     =
INCLUDES      =

#DEBUG=on

sandbox_SOURCES = sandbox.cpp
cmsis_SOURCES = cmsis.cpp

include $(EMBEDDED_ROOT)/include/Makefile.mk

all: do_all
	@echo " . . . 'all' done" 

install: do_install
	@echo " . . . 'install' done" 

upload: do_upload
	@echo " . . . 'upload' done" 

clean: do_clean
	@echo " . . . 'clean' done" 

#___oOo___
